{
  "name": "nft_market",
  "version": "1.0.0",
  "description": "This is an example nft market project. User can create his own nft, add a picture and set initial USDC price.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\"",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,ts,json,css,html,sol,md}": [
      "prettier --check"
    ]
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3"
  }
}
